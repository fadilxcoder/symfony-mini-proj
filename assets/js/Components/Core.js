$(function () {
    Core.showTheText();
});

var Core = {
    newsletterTxt : $('#newsletter').find('.widget-body p').text(),

    showTheText: function () {
        // console.log(Core.newsletterTxt);
    }
};